# Gemini Memory
* When compiling apps (like SPEC benchmarks) for the `real_machine_checkpoint` project, the workflow is ALWAYS: **Local Compilation** (using the ad-hoc Docker container) -> **Remote Upload** (sync to Altek for execution). Do NOT compile them on Altek directly.
